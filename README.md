# JSONify: Dynamic JSON Validator, Formatter & Schema Mapper

Validates JSON syntax, formats with indentation, and extracts keys dynamically.

## Features
- Single-file lightweight script execution context.
- Modern CSS variables / clean Plotly integration.
- Ready to host on static hosting platforms.

## Setup & Running
Open `index.html` directly in any web browser.
